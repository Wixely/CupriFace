# CupriFace Roadmap

The core milestones **M0–M9 are complete** (engine, layout, text, paint, binding, components,
interaction, Windows a11y (UIA), AOT trim-clean, WASM host) — see [DESIGN.md §12](DESIGN.md).
This document tracks everything **considered but not yet implemented**: the deferred refinements
called out in the design plus items we've since scoped. Rows that have since shipped stay, marked
🟢 with what proves them — a roadmap that quietly deletes its own history stops being checkable.
It is a living list, not a commitment of dates.

**Status legend:** 🔴 not started · 🟡 partial / scaffolded · 🟢 prototyped, needs hardening
**Priority:** **P1** (core to the product's promises) · **P2** (important) · **P3** (nice to have)

---

## 1. Accessibility bridges (P1)

The platform-neutral **semantics tree** is real (identity, focus, states, on-screen bounds), and
**four platforms now have working bridges** — UIA (Windows), AT-SPI (Linux), NSAccessibility
(macOS) and TalkBack (Android, §11) — each with a blocking CI gate driven by a real
assistive-technology client, plus the web's hidden-DOM mirror. What remains is refinement inside
each, and a human screen-reader pass.

| Item | Status | Notes |
|------|--------|-------|
| Complete **UIA** provider (Windows) | 🟢 | **Done (2026-08), proven by a blocking CI gate.** The window answers `WM_GETOBJECT` with a UIA fragment tree built from the semantics tree — on BOTH Windows paths (the GL window and the SDL software window, which is what GPU-less machines — RDP, VMs, the CI runner — actually run). Patterns per role: Invoke (button/link/menuitem), Toggle (checkbox/switch), SelectionItem (radio/tab/option/treeitem), RangeValue (slider read+write, progressbar/spinbutton read), Value (textbox/combobox, read for now), ExpandCollapse (`aria-expanded`), Selection (containers). Threading: UIA calls read an immutable snapshot published after each drawn frame; actions queue to the UI thread and run through the same machinery as a real click (`AccessibilityActivate`/`Focus`/`SetValue`, path-addressed so handles survive the per-keystroke rebuild); focus changes raise UIA focus events (what makes Tab talk). Names include positional labels — the same sibling association clicks use, so Narrator's announcement and the click target can't disagree. The gate (`tests/UiaSmoke`, FlaUI — a real UIA client): tree served, a named checkbox toggles, a slider answers a RangeValue write, Tab focus visible. Interop is one file mirroring UIAutomationCore.idl (the WindowsDesktop managed wrapper would ~4× a self-contained publish); `CUPRIFACE_UIA=0` is the kill switch. Remaining inside this item: Text pattern for editable fields, Value.SetValue, UIA events beyond focus (property/structure changed). One caveat stated plainly: the gate exercises the SDL path (the runner is GPU-less, like every RDP/VM box) — the GL window carries identical wiring but no CI machine can run GL-on-Windows to prove that combination. |
| **AT-SPI** bridge (Linux) | 🟢 | **Done (2026-08), proven by a blocking CI gate.** The app joins the accessibility bus as an AT-SPI application (`org.a11y.Bus.GetAddress` → connect → `Socket.Embed` with the registry) and serves the semantics tree over D-Bus: Accessible (roles, states, attributes, relations), Component (screen extents, hit-testing), Action (click/toggle), Value (slider read **and** write), Application, and **Cache** — the last is how libatspi bulk-loads a tree, and without it a client sees the app and nothing inside it. ARIA→AtspiRole maps in the same table shape as the UIA bridge, so a control that reads correctly to Narrator reads correctly to Orca. Threading is identical to the Windows leg because the hazard is: D-Bus calls read an immutable snapshot published after each drawn frame; actions queue to the UI thread and run through the ordinary click machinery (`AccessibilityActivate`/`Focus`/`SetValue`, path-addressed so ids survive the per-keystroke rebuild). Change notification: `StateChanged` per flipped state bit, `PropertyChange` on rename, focus on **both** ends of a move, and `Cache.AddAccessible`/`RemoveAccessible` for structure — which matters more here than in most toolkits, since CupriFace rebuilds the tree per keystroke, making structural churn the common case rather than the rare one. **Portability: zero interop.** AT-SPI is D-Bus over a Unix socket, not an OS API, so unlike `UiaInterop.cs` this bridge contains no P/Invoke and no native library — Tmds.DBus.Protocol is ~240 KB of managed IL that publishes trim-clean, and a Windows app carries it without ever loading it. The gate (`tools/atspi-gate.{sh,py}`, pyatspi over libatspi — the channel Orca uses) stands up a private a11y bus and registry under xvfb and asserts: the tree is served with real roles and screen extents (46 accessibles on the showcase), DoAction genuinely flips a checkbox, that flip arrives back as `object:state-changed:checked`, Value.Set moves a slider, and the tree still answers after an activation rebuilds it. `CUPRIFACE_ATSPI=0` is the kill switch, `CUPRIFACE_ATSPI_DEBUG=1` traces the conversation. Remaining inside this item: Text interface for editable fields, Selection on containers, and honouring the registry's event-listener registration (we currently emit unconditionally rather than only when something is listening). Same caveat as Windows: the GPU-less runner takes the SDL software path, which is what an RDP/VM user gets — the GL window carries identical wiring, but no CI machine proves that combination. |
| **NSAccessibility** bridge (macOS) | 🟢 | **Done (2026-08), proven by a blocking CI gate.** macOS asks the window's content VIEW for accessibility, and that view belongs to SDL — so the bridge builds a subclass of whatever class the view actually is **at runtime** and re-points that one instance at it (`object_setClass`, the mechanism KVO uses): our methods answer, everything else falls through to SDL, and no other view in the process is touched. Each node gets a small Objective-C object created the same way, holding only its node id in indexed ivars; ids are per structural path and never reused, so an element VoiceOver holds keeps meaning the same control across the per-keystroke rebuild. Serves role + subrole (a `switch` is an `AXCheckBox` with the `AXSwitch` subrole; a `tab` is an `AXRadioButton` in an `AXTabGroup` — genuinely how Cocoa models both), label, value with min/max, parent/children, frame, enabled/focused, `AXPress`, and `setAccessibilityValue:`; focus moves post `NSAccessibilityFocusedUIElementChangedNotification`. Actions queue to the UI thread and run through the ordinary click machinery, exactly as the other two do. **Interop is one quarantined file** (`ObjC.cs`) — a CI probe established first that runtime class creation with `[UnmanagedCallersOnly]` implementations works through libobjc with **no compiled Objective-C shim**, which is what kept macOS from becoming the first platform to force a hand-written native library into the project. The gate (`tools/nsa-gate.{sh,py}`, pyobjc over the AX client API — the channel VoiceOver uses) asserts what the bare window cannot produce: 155 AX elements including the app's own named controls, `AXPress` genuinely flipping a checkbox, `AXValue` moving a slider, and the root filling the window's content area **in points**. That last one is not decoration: it caught the engine's logical units reaching AppKit unscaled (a 30-unit button claiming 30 points instead of 26.9, an error that grows with distance from the origin). `CUPRIFACE_NSA=0` is the kill switch, `CUPRIFACE_NSA_DEBUG=1` traces attach and geometry. Remaining inside this item: `accessibilityHitTest:` (VoiceOver's mouse-follow), text ranges for editable fields, and value/structure notifications beyond focus. |
| **Web hidden-DOM a11y** | 🟢 | **Done** — `CupriDocument.BuildAriaHtml` serialises the semantics tree to an ARIA fragment (`role` + `aria-label`/`aria-checked`/`aria-valuenow-min-max`/`aria-disabled`/`tabindex`); the WASM host mirrors it into an off-screen, screen-reader-visible `<div>` (clip pattern, `aria-live`) beside the `aria-hidden` canvas, updated on input-driven repaints. Verified headlessly (roles/labels/states + updates on model change). |
| End-to-end screen-reader verification of the control gallery | 🟡 | All four platforms are automated-client verified (FlaUI over the Narrator channel, pyatspi over Orca's, pyobjc over VoiceOver's, uiautomator over TalkBack's), which proves the tree, the roles, the states and that actions change the app. What no automated client can judge is what a human hears: announcement wording, reading order, live regions. That pass — real Narrator/NVDA, real Orca, real VoiceOver, real TalkBack — hasn't been done on any of them. |
| Off-screen nodes are announced as visible | 🟢 | **Done (2026-08), gated on all three platforms.** The shared semantics tree now follows the clip chain — the viewport, then every `overflow` ancestor, using the SAME condition the painter uses to emit a `PushClip`, so what paints clipped and what reads as off screen cannot disagree — and marks each node `Offscreen`. Any overlap counts as visible: a control half scrolled into view is one the user can see and reach. Each bridge then surfaces it the way its platform expects: UIA's `IsOffscreen` property, AT-SPI's SHOWING dropped while VISIBLE stays (the exact distinction that pair of states exists for), and on macOS element hiding — an off-screen node stops being an accessibility element and the system splices it out. Four engine tests cover the geometry (below the viewport, clipped by a scroller, straddling the edge, plainly visible); each gate proves the end of it against a real AT client, the macOS one by asserting that the 88 reachable-but-off-screen controls it used to count are now zero. |

## 2. CSS engine (P2)

Ambition is the modern core; these are the sequenced-but-unshipped pieces (DESIGN §8, §12 caveats).

| Item | Status | Notes |
|------|--------|-------|
| **CSS transitions** | 🟢 | **Done** — `transition: <prop> <dur> [easing] [delay]` (comma-separated) animates `opacity`, `background`/`color`/`border-color`, `transform` and **`filter`** when their target value changes (hover, class/model change, theme toggle). Easing keywords (`linear`/`ease`/`ease-in`/`ease-out`/`ease-in-out`) **and `cubic-bezier(x1,y1,x2,y2)` literals** (incl. overshoot), via a Newton cubic-bézier solver; parsing is paren-aware so a `cubic-bezier()`'s inner commas don't break the list. `filter` chains interpolate op-by-op (a missing side pads to identity filters; mismatched chains fall back to discrete). State keyed by structural path so it survives the per-interaction rebuild. Paint-only (no relayout), driven by the same per-frame `Animate` clock as `@keyframes`. Follow-ups: width/height (layout-affecting) transitions, per-property longhands. |
| Real **`:hover` / `:active`** pseudo-classes | 🟢 | **Done** — authored as `:hover`/`:active`/`:focus` (rewritten to marker attributes the engine toggles). `:hover` follows the pointer; `:active` marks the pressed element chain on pointer-down and clears on pointer-up (holds while held, like a real button press). |
| **`filter`** | 🟢 | **Done** — `filter: blur() brightness() contrast() grayscale() saturate() sepia() invert() opacity() drop-shadow()`, composed into one `SKImageFilter` (colour-matrix ops fold into a colour filter; blur/drop-shadow are image filters) and applied via a `SaveLayer` wrapping the element's subtree. Animatable via `transition` (op-by-op). |
| **Border style keywords** (`solid`/`dashed`/`dotted`) | 🟢 | **Done** — `border-style` (and the keyword in the `border` shorthand) parsed to `BorderLineStyle`; the rasteriser dashes/dots the uniform border stroke via a dash path effect (`none`/`hidden` suppress it). |
| Grid: **named lines** + **multi-row spans** (`rowSpan>1`) | 🟢 | **Done** — `grid-template-columns/rows` parse `[name]` line names into a name→line map; `grid-column`/`grid-row` accept named lines (`main-start / main-end`) resolved at layout time, and `grid-row: span N` spans multiple rows (row heights summed + inter-row gaps). |
| **Inline formatting context** (rich inline flow) | 🟢 | **Done** — a block with ≥2 consecutive inline-level children runs a real IFC: text and inline / inline-block elements flow into wrapping line boxes (word wrap, per-run fonts/weights/colours, mixed sizes centred per line, shrink-to-fit inline-blocks). Inter-run whitespace is preserved via `WsBefore`/`WsAfter` (no whitespace-only DOM nodes, so flex/grid are untouched); a lone inline child keeps the original per-node path (fields/textareas unchanged). Follow-ups: painted background/border/padding on a wrapping inline element (currently only its text flows), true baseline alignment, and `text-align:justify`. |
| Full **bidi line-breaking** (UBA) | 🟡 | Simplified bidi reorder ships; the full Unicode Bidi Algorithm remains. |

## 3. Text & internationalization (P2)

| Item | Status | Notes |
|------|--------|-------|
| International + **emoji font fallback** | 🟢 | **Done** — `FontService.SplitRuns` splits text (codepoint-aware, surrogate pairs intact) into runs by which face can render each character: the primary family, or a fallback found via `SKFontManager.MatchCharacter` (cached per codepoint). The rasteriser draws, and `MeasureText` measures, each run in its own HarfBuzz‑shaped face — so emoji/CJK/symbols the primary lacks render instead of tofu (subject to a suitable face being installed). |
| Full spec **UBA** bidi | 🟡 | See §2 — current reorder is simplified. |

## 4. Performance & threading (P1)

Sustained fluidity is the core requirement (DESIGN risk #0); a demo hitting 60 fps isn't enough.

| Item | Status | Notes |
|------|--------|-------|
| Render-thread split in the **interactive windows** | 🟢 | **Done for the CPU/SDL path.** `CupriDocument.BuildFrame` is the UI-thread half (layout + paint → immutable `DisplayList`, no rasterise); a new `ThreadedPresenter` (over `ThreadedRenderer`) rasterises off-thread and hands back the latest frame via one lock-guarded buffer. `DesktopHost` uses it for the SDL software window when `CupriApp.ThreadedRender` is set. Verified headlessly: threaded raster matches single-threaded pixels, `Commit` never blocks + coalesces latest-wins, and `ThreadedPresenter.Present` draws the correct frame. Remaining: the GL path renders inline (threading it needs a background GL context — the per-backend piece). |
| **List/tree virtualization** | 🟢 | **Done (paint-culling).** A scroll container skips painting children whose box is entirely outside the visible band (+margin), so a long list costs paint+raster for the on-screen rows only — the win during scroll. Layout is untouched (culling is paint-only); transparent, no API/markup change. Follow-up: also skip layout for off-screen rows (needs app-cooperative windowing). |
| **CI perf-budget gates** | 🟢 | **Done** — `PerfBudgetTests` runs in the existing CI job (`dotnet test -c Release`), so a regression fails the build. CI hardware is shared and variable, so the gates are **ratios measured inside one process** (a document against a variant of itself, sampled interleaved and taken as medians) rather than absolute milliseconds — machine speed cancels, and each ratio encodes an invariant: styling doesn't scale with stylesheet size, hovering doesn't either, rebuilding doesn't scale with hidden content, a hover costs less than a rebuild, an unchanged frame presents nothing, and a move inside one element is a no-op. Each was verified to FAIL when its optimisation is reverted (×3.95, ×5.17, ×2.17). One deliberately loose wall-clock backstop (400 ms) catches only the catastrophic; a tight absolute number on a noisy runner is a flaky gate, and a flaky gate gets disabled. |
| **Live-resize fluidity** during the OS modal resize loop | 🟢 | Reflow itself is instant, and BOTH backends now have the streaming machinery: Windows and macOS run a modal loop while a window edge is dragged, so the render loop gets no turn until the mouse is released and a frame left to "next tick" arrives when the drag *ends*. The GL window therefore repaints from GLFW's framebuffer-resize callback (`DoRender` inside `OnFramebufferResize`), and the SDL window from an `SDL_AddEventWatch` callback, which SDL dispatches synchronously from inside that modal loop. **What was missing was not the code but any way to know it works** — the GL path swallowed re-entrancy failures in a bare `catch {}`, so it could do nothing and look identical. Both paths now count the frames they paint (`ResizeFrames`), `CUPRIFACE_RESIZE_DEBUG=1` traces each one, and a refused repaint prints once instead of vanishing. **Confirmed (2026-08) by a human drag on Windows, GL window**: frames stream during the drag rather than arriving on release. That last step resists automation and always will — an external `SetWindowPos` is not the modal loop, and nothing can hold a mouse button down inside one — so this row rests on a person's eyes plus the trace, not on a gate. Not yet drag-confirmed: the SDL software window and macOS. Both carry the same counters and the same `CUPRIFACE_RESIZE_DEBUG=1` trace, so each is a one-drag check whenever someone is in front of one. |

## 5. Web / WASM target (P2)

| Item | Status | Notes |
|------|--------|-------|
| **Prompt-free clipboard** via a hidden focused `<textarea>` | 🟢 | **Done (web keyboard path).** Keyboard focus + clipboard now flow through a hidden `<textarea>`: Ctrl+C/X/V fall through to native `copy`/`cut`/`paste` events (engine selection supplied via `clipboardData`; pasted text fed to the engine) — no `navigator.clipboard`, no prompt, no `readText` automation wedge. The right-click-menu Paste still uses `navigator.clipboard` (a menu click can't raise a native paste event) — a deliberate, user-initiated path. |
| **NativeAOT-LLVM host builds on Windows AND Linux** | 🟢 | **Done (2026-08), CI-verified on both.** Three Windows-isms had to go, each hidden behind the previous: `IlcHostPackagePath` was pinned to `win-x64` (now derived from `$(NETCoreSdkPortableRuntimeIdentifier)`, with the ILC version pinned so the package path is deterministic); the static Skia/HarfBuzz `.a` archives were addressed by a hand-built `$(NuGetPackageRoot)` path with **no PackageReference at all**, so the build only ever worked on a machine where WebWasm had already filled the shared NuGet cache; and `ExecutableExtensionName` was unconditionally `.exe`, so the Linux link asked for `node.exe`. None of these were visible locally — a clean machine was the only way to find them, which is what the new `webllvm` CI job is for. |
| **LLVM host: typing + editing + clipboard shakedown** | 🟢 | **Done (2026-08), exercised in a real browser.** Typing inserts; Backspace deletes; Home/arrows move; Shift+arrow selects (and the selection paints). `CopySelection` returned "Ada Lovelace", `CutSelection` returned it and left the field empty, `PasteText` inserted "Grace Hopper", `Undo` removed it, `Redo` restored it. Driven by calling the exports directly rather than by pressing Ctrl+C/V, because automating a real clipboard read wedges the tooling — so the one link still unverified is the browser's native `copy`/`cut`/`paste` event handing off to these exports (the listeners are wired in main.js; the engine side is now proven). `globalThis.__cupri.M` exists as the diagnostics handle that made this testable. |
| **Promote WebLlvm over WebWasm** | 🟡 | Neither the cross-platform build nor the editing path blocks this any more (both above). What remains is a supply-chain judgement, not a bug: the host needs the `dotnet-experimental` preview feed (pinned, so it cannot drift), where WebWasm needs only the stock SDK + wasm workload. |
| **IME composition** on the web host | 🟢 | **Done (2026-08), alongside the Android IME work (§11) — one engine seam serves both.** The engine gained a real preedit: `SetComposition`/`CommitComposition`/`ClearComposition` hold the marked range inside the edit buffer (the model only ever sees committed text; one undo step per composition; Escape clears, any other key commits-then-acts, blur commits; underline painted), plus code-point-aware editing so a committed emoji deletes as one character. Both web hosts wire it: `compositionstart/update/end` on the hidden keyboard `textarea` drive the engine, the keydown path ignores events mid-composition (`isComposing`/keyCode 229 — the gate that used to steal keys from the IME), the hidden textarea repositions to the engine's caret rect, and `inputmode` follows the focused field's kind. The EditKey table is now fetched from the host instead of hand-duplicated in JS. Proven by fifteen headless engine tests; browser-side, scripted CompositionEvents drive the listeners — a REAL OS IME in a browser resists automation, so that last inch is a documented manual pass, same standard as the Android one. |
| WASM **a11y** | 🟢 | **Done** — the off-screen ARIA mirror (see §1). |
| Fix Mono **WASM-AOT codegen** so `CupriFace.dll` can be AOT-compiled | 🟡 | Retested on SDK **10.0.302** (2026‑08): still traps at boot (`function signature mismatch`). Symbolised precisely this time: the fault is **inside `SliderComponent.Expand`'s AOT body** (its first AngleSharp `IElement` call), *not* the registry→component dispatch — devirtualising the call site was tried and the trap stayed at the same frame, so "refactor the offending dispatch" is off the table; any component's `Expand` would trap (Slider is merely registered first). Interface calls made before expansion (`ICupriComponent.Tag`) dispatch fine → signature‑specific Mono codegen bug. Cost of the interpreted workaround, measured in‑browser (940×720 showcase): hover restyle **16.2 ms**/move, section switch **60.7 ms** (~8× native) — the single biggest web‑perf lever left. Needs an upstream fix; re‑try each SDK update (the csproj comment carries the repro). **Alternative path examined (2026‑08): NativeAOT‑LLVM** (runtimelab, the backend Bootsharp rides) — a different compiler + runtime, so the Mono bug cannot occur and the whole engine would compile; Bootsharp's own bench (.NET 10) puts LLVM compute at ~1.2× Rust vs Mono‑AOT's ~2.1×, and we're currently *interpreted*. Costs: no Mono runtime means no `[JSImport]/[JSExport]`/`dotnet.js` — the host's ~15 exports would move to `UnmanagedCallersOnly` + `DllImport` + an Emscripten `--js-library` (contained; the zero‑copy present gets simpler, JS reads `HEAPU8` at a pointer). **Probe run (2026‑08): PASS.** A hello‑world on `ILCompiler.LLVM 10.0.0-*` statically linked SkiaSharp 3.116.1's `libSkiaSharp.a` (the `3.1.56/st` variant, matching the workload's Emscripten) via `NativeLibrary` + `DirectPInvoke`, under `TrimMode=full`, and rendered pixel‑exact under Node (cleared copper, drew a steel‑blue rect, read both back correctly). Output: one 6.9 MB `dotnet.native.wasm` (at `-O1`, before Binaryen) + small JS loaders — a simpler deployment shape than the Mono build. Known follow‑ups for a real port: an `__wasm_longjmp` undefined‑symbol warning at link (Skia codec error paths — needs the emcc longjmp flag), HarfBuzz linking untested, and the host's ~15 interop exports must move off `[JSImport]/[JSExport]`. Wiring reference: `elringus/bootsharp` → `samples/bench/dotnet-llvm/DotNetLLVM.csproj` (`Microsoft.DotNet.ILCompiler.LLVM 10.0.0-*` from the `dotnet-experimental` feed). Trim posture is favourable: desktop ILC analysis is already clean and binding is source‑generated. |

## 6. AOT & build (P2)

| Item | Status | Notes |
|------|--------|-------|
| Native **link** of the AOT desktop build in CI | 🟡 | ILC trim analysis is clean (0 warnings); the final link needs a box with the C++ toolchain on PATH. Note: the UIA bridge's COM interop (`ComImport`) is unsupported under NativeAOT — `TryAttach` fails gracefully there (app runs, no UIA); an AOT-compatible bridge means a ComWrappers rewrite (DESIGN.md's known sharp edge). |
| Publish matrix across the three desktop OSes | 🟢 | **Done (2026-08)** — `test` and `viewer` run on ubuntu / windows / macos-latest; every leg publishes a standalone single-file build and smoke-tests it AT RUNTIME on its own OS (Linux twice: SDL and the blocking GL gate; Windows: launch + the blocking UIA gate; macOS: both render paths). Release packs win-x64 exe + linux-x64 and osx-arm64 tar.gz. |
| **macOS window path segfaulted on GL-less virtual Macs** | 🟢 | **Fixed (2026-08), proven in CI.** The macOS crash report named it: `glfwCreateWindow` fails on a Mac with no OpenGL (the paravirtual GPU of virtualised Macs — CI runners are `VirtualMac2,1`) *without setting a GLFW error*; Silk.NET then applies its default window position to the NULL handle, and release-build GLFW (asserts compiled out) dies with SIGSEGV inside `glfwSetWindowPos` — before any managed guard can run, so uncatchable in-process. Fix: on macOS the host first spawns itself as a disposable probe child (`--cupriface-gl-probe`) that attempts full GL bring-up and reports by exit code; if the child dies, only the child dies, and the real process goes straight to the SDL software window. Real Macs (GL present) pay one invisible ~200 ms probe and keep the GPU window. The macOS CI smoke runs BOTH paths every push. |
| **Linux window path segfaults on a GPU-less/virtual X server** | 🟢 | **Fixed (2026-08), proven by a blocking CI gate.** Root cause, caught by the `CUPRIFACE_GL_DEBUG` loader trace running on the failing machine itself: Skia probes for **EGL** entry points (`eglQueryString`, `eglGetCurrentDisplay`) through whatever loader it is given, and ours forwarded every name to GLFW — on X11 that is `glXGetProcAddressARB`, which is *specified* to fabricate a dispatch stub for any name it does not recognise. It returned confident non-null garbage for the egl* probes, Skia concluded EGL existed, called the stub, SIGSEGV. Null would have been handled (Skia null-checks and skips EGL detection) — the lie killed, not the lack. Fix: the loader answers only for `gl*` names (`SkiaWindow`). The CI Linux job now runs the GL path under xvfb+llvmpipe — the exact configuration that used to die — as a **blocking** smoke test with the trace left on, so any regression names its killer. `CUPRIFACE_SOFTWARE=1` remains as a deliberate escape hatch. Two failed attempts are recorded in git history (a `glGetString` null-guard; assuming the probe call itself was the problem) so they are not retried. |

## 7. Controls & component library (P3)

| Item | Status | Notes |
|------|--------|-------|
| **Right-click context menu for inputs** | 🟢 | **Done** — Cut / Copy / Paste / Select-all on right-click in text fields & textareas. Engine-owned overlay: `DispatchContextMenu(x,y)` opens a self-styled `position:fixed` menu at the pointer (Cut/Copy greyed when there's no selection); items raise `ContextRequested`, which each host routes through the SAME clipboard seam as its keyboard shortcuts (desktop SDL/GLFW `MouseButton.Right`, web `contextmenu`). Dismisses on outside-click / wheel / keystroke / Escape. |
| **Extensibility for custom interaction primitives** | 🟢 | **Done** — `CupriDocument.OnAction("data-…", handler)` registers a custom activation primitive: a clicked or keyboard‑activated element carrying that `data-*` attribute invokes the handler (`CupriActionEvent`: element, value, model, x/y), checked before the built‑ins and firing on both click and Enter/Space. Third parties can define new `data-*` interactions without an engine change. |
| CSS-controllable **icon sizing** | 🟢 | **Done** — icons render `width/height: var(--cupri-icon-size, <per-use default>)` and carry a `cupri-icon` class hook, so authors restyle size from CSS (set the token on an ancestor or target `.cupri-icon`) without fighting the inline width; the per-use size remains the default. |
| **Images** (`<cupri-image>`) | 🟢 | **Done** — SkiaSharp decode + `DrawImage` command; `src` via `CupriSource` (embedded/file/URL) or `data:` URI; `object-fit` contain/cover/fill/none; intrinsic + aspect sizing. **Remote (`http(s)`) images now load asynchronously** on a background task (they never block the first paint; the image pops in when it arrives — `ConsumeImageArrived()` flags the repaint, de-duped per src). Local (embedded/file/`data:`) decode synchronously. URL policy is configurable via `UseImageUrlOptions`. |
| **Video** | 🟢 | **Done (2026-08).** `<cupri-video src fit poster label controls autoplay muted loop>` — sources resolve exactly like images (embedded / disk / `data:` / policied https, one shared `SourceResolver`), sized like `<cupri-image>`, with engine-drawn accessible controls and true element fullscreen (⛶ expands the video over the viewport in the top layer AND takes the window OS/browser-fullscreen via the `WindowCommandRequested` seam; Escape or ⛶ undoes both). Three tiers: the **engine** gained a generic live-surface lane (`data-cupri-surface` + `SurfaceRegistry`, reused later by 3D) that rides the image pipeline's own DrawImage/object-fit/damage path, plus `ClearHole` for host-composited surfaces; **both web hosts** (Mono/WebWasm via `[JSImport]`, NativeAOT-LLVM via the C ABI + Emscripten JS library) let the BROWSER decode into an underlaid `<video>` beneath a transparent hole (≈0 wasm bytes of codec, hardware decode, rect+clip synced per painted frame); the optional **`CupriFace.Media`** package plays WebM on desktop — managed EBML/Matroska demuxer (pure C#, unknown-size clusters and all three lacing modes) plus libvpx/libopus statically linked into ONE `cupricodecs` library per RID — **six of them** (win-x64/arm64, linux-x64/arm64, osx-x64/arm64) inside a single nupkg (the Skia model: build anywhere, run anywhere). Audio out rides the SDL binding the shell already ships — no new native dependency. Verified: real-codec decode + paint tests, a browser pass on EACH web host (play/pause/scroll-clip/fullscreen on WebWasm; boot/decode/hole/click-to-toggle on WebLlvm), the Showcase playing on desktop, and a CI gate that builds MediaProbe on Windows and runs those SAME binaries on **all six RIDs** — x64 and ARM Linux, Intel and Apple-silicon macOS, x64 and ARM Windows — each leg being the only place that library ever executes (the build jobs check symbols, not behaviour), plus an import-table check that the Windows DLLs carry no MinGW runtime dependency. The controls bar carries a real **seek slider** (pointer scrub, arrow keys ±5 s, AT RangeValue.SetValue) with position/duration clocks, reflected ~1×/s during playback without disturbing the frame fast path. Remaining: AV1 (dav1d), buffering/streaming sources. |
| **Audio (standalone)** | 🟡 | Playback exists as part of video and is tested against a VP9+Opus fixture (demux → libopus → PCM; sink start/feed/pause/flush; mute as a gain of zero). **A/V sync over minutes is now proven, the hard way**: a 3-minute soak (`MediaProbe --soak`, `SDL_AUDIODRIVER=dummy` for a realtime-draining device on headless boxes) measured +27 ms/s of drift under the wall clock — a browser-recorded clip carrying ~2.7% more PCM than its timeline claims — so the player now runs an **audio-master clock** (media time = audio actually played, submitted − queued, wall handoff when audio ends); the same soak then held 0.0 ms of lag end to end with zero underruns. Still missing: a first-class `<cupri-audio>` element and non-video containers. |
| Additional controls | 🟡 | **Hover tooltip + typeahead combobox + date picker done.** `<cupri-tooltip>` shows on `:hover`. `<cupri-combobox>` is a filtering typeahead. `<cupri-datepicker value="{{Date}}" open="{{Open}}">` is a date field (ISO `yyyy-MM-dd`) with a month-calendar popup — a day pick writes the value + closes; ‹ › page months in place (the new generic `data-set-keep` attribute sets a bound value without dismissing the overlay). **Combobox keyboard nav done** — Down/Up highlight suggestions (a generic focused‑`data-listbox` mechanism), Enter commits the highlighted one, Escape closes. Datepicker: **arrow-grid day nav** — a generic `[data-gridnav="cols"]` overlay captures arrows (±1 day / ±cols week), highlights the day, Enter picks it, Escape closes. **Sortable table done** — `<cupri-table sort="{{Sort}}">` makes header cells click‑to‑sort (numeric or case‑insensitive text, asc↔desc, ▲/▼ marker), reordering body rows on expand. **Time picker done** — `<cupri-timepicker value="{{HH:mm}}" open="{{Open}}">` with scrollable hour/minute columns (picking a part keeps the popup open via `data-set-keep`). Long lists paint-cull (§4 virtualization) so the columns stay cheap. Remaining candidate: virtualized rows for the data table via the same paint-culling (already applies to any scroller). |
| Visual **debug overlay** | 🟢 | **Done** — `CupriDocument.DebugOverlay` (or `CupriApp.DebugOverlay`) outlines every element's border box on top of the paint, scroll containers in a distinct colour, for inspecting layout in the live window. |

## 8. Tooling & quality (P2)

| Item | Status | Notes |
|------|--------|-------|
| **Automated test project** (xUnit) + CI | 🟢 | **Done** — `tests/CupriFace.Tests` (xUnit) with a shared `TestDoc` harness; 33 tests covering transitions (+cubic-bezier, +filter), `filter`, `border-style`, `:hover`/`:active`, the single-line field (nowrap + horizontal scroll), wrap-aware selection, the context menu, `RenderToPixels` (premul/straight/transparent), async `ImageStore` loading, and the native controls (tabs, accordion, select, textarea, tree, checkbox, switch, radio, slider). GitHub Actions runs `dotnet test` on push/PR. |
| **Refresh `comparisons/`** | 🟢 | **Done (2026-08).** Avalonia / Electron / MewUI re-checked against the repository after the Android host landed: mobile rows (Android real, iOS absent), accessibility rows (four gate-proven bridges, not one), IME rows (composition shipped — it was listed as missing in all three), test count 270 → 369, element count ~59 → 69. Where a document's *conclusions* moved, they were rewritten rather than patched: "choose the other one because CupriFace has no Linux/macOS screen-reader bridge" was true and is now false, and the honest replacement is the soak-time gap, not the coverage gap. |
| **`comparisons/maui.md`** — CupriFace vs .NET MAUI | 🟢 | **Done (2026-08).** The comparison people ask for now that CupriFace runs on Android. Structured around the one difference everything follows from — MAUI draws with the platform's native controls, CupriFace draws everything itself identically everywhere — and honest about which side that favours per axis: MAUI wins iOS, platform APIs, native text input, mature mobile a11y, tooling and first-party support; CupriFace wins Linux, the browser, pixel-identical UI, headless-testable views and render-into-anything. Includes a Blazor Hybrid section (the "but I want HTML in MAUI" answer: it ships a WebView, which is the thing this project exists to delete) and the CoreCLR-vs-Mono runtime note stated fairly — our engine triggers the defect; Mono ships countless working MAUI apps. |

## 10. Text input polish

| Item | Status | Notes |
|------|--------|-------|
| **Single-line field: `white-space:nowrap` + horizontal caret-follow** | 🟢 | **Done** — a `<cupri-textfield>` now stays one line (was ballooning when a long/pasted value wrapped): `white-space:nowrap` lays the value on a single line, `overflow:hidden` clips it, and a per-field `ScrollX` follows the caret so the visible window tracks typing/navigation (preserved across rebuilds like `ScrollY`). `white-space` is a real inherited property (`nowrap`/`pre` supported). |

## 9. Embedding & overlays (P3)

Rendering *over* other content — the desktop, a game, an HTML page — so the UI can be composited by a host.

| Item | Status | Notes |
|------|--------|-------|
| **Transparent / frameless / top-most windows** | 🟢 | **Done** — `CupriApp.Transparent`/`Frameless`/`TopMost`; the GL host opens a transparent framebuffer + transparent clear (premultiplied output = what compositors want), the SDL fallback honours frameless/top-most (opaque). Portable Silk.NET/GLFW traits, no OS-specific code. Needs a compositing WM (universal on modern OSes); degrades to opaque otherwise. [`samples/TransparentHud`](samples/TransparentHud/). |
| **Web canvas overlay** | 🟢 | **Done** — transparent clear + straight-alpha present for `putImageData`; the JS glue passes pointer events through wherever nothing is drawn. |
| **`RenderToPixels` embed primitive** | 🟢 | **Done** — RGBA8888 `byte[]`, premultiplied or straight alpha, for blitting into any host surface (game texture, another render target). |
| **Unity / game-engine embed sample** | 🔴 | The pieces exist (`RenderToPixels` straight-alpha → `Texture2D.LoadRawTextureData`); a worked sample + input-forwarding shim is the follow-up. |
| **SDL software-path transparency** | 🔴 | The GL path is transparent; the CPU/SDL fallback blits opaque. Per-pixel alpha against the desktop on the software path is a per-OS follow-up (deliberately deferred as "too OS-specific"). |

## 11. Mobile (Android) (P1)

The engine runs on `net10.0-android` **unchanged** — same portability law as everywhere else: the
platform work lives in a host package (`src/CupriFace.Android`), the engine gained only portable
capabilities (touch gestures, fling physics, IME composition) that desktop and web share. Every
row below is proven by the Android leg of the CI gate: a real emulator, a real APK, taps computed
in the app view's own published coordinate space, assertions read back over logcat markers and
uiautomator dumps. Feasibility forensics (including why Mono is off the table) live in
`samples/AndroidProbe/` — kept as a diagnostic, superseded as a host.

| Item | Status | Notes |
|------|--------|-------|
| **`CupriFace.Android` host package** | 🟢 | **Done (2026-08), gate-proven.** `CupriActivity` (author API: `CreateApp()` + optional `ConfigureDocument`) + `CupriHostView` (an `SKGLSurfaceView` — the same GPU model as the desktop GL window) + `AndroidHost` (the `DesktopHost` analogue: clipboard seam, `Navigated`→`ActionView`, fullscreen→immersive, app stack with Back popping, surface-loss recovery). The one rule everything follows: **the GL thread is the document thread** — UI-thread events cross via `QueueEvent`, UI-thread reads come from an immutable post-frame snapshot. Render-on-demand end to end: `WhenDirty` + dispatch-driven `RequestRender` + self-chaining while animations run. `Console` output bridges to logcat (CoreCLR-Android has no stdout destination — a portable app's diagnostics would silently vanish). |
| **CoreCLR mandatory** (`UseMonoRuntime=false`) | 🟢 | The package's buildTransitive targets default every consumer to CoreCLR. Not preference — necessity: Mono 10.0.11 has a codegen defect (string content dereferenced as a string reference; device-tombstone forensics in `samples/AndroidProbe/MONO-CRASH.md`). Re-check on future Mono releases before ever relaxing it. |
| **Engine touch layer** (portable) | 🟢 | **Done (2026-08).** `TouchInput` — a headless-testable gesture recognizer: tap activates on finger-UP behind an 8px slop (the engine's `DispatchClick` is pointer-down, which a finger must not trigger on contact), explicit drag affordances (slider/scrollbar/reorder grips…) drag immediately, scroll reuses `DispatchWheel`'s chaining/edge behaviour, long-press opens the context menu, double-tap escalates clicks, and release velocity hands off to **fling in the document**: `StartFling`/`StepFling` integrate beside the other animation sources (exponential decay, edge clamp, no ancestor chaining, re-windows `cupri-virtual` as it coasts). Gate-proven: one activation per tap (marker alternates True/False, exactly), momentum beyond the finger's travel (settle marker), and the web host can adopt the same class later (recorded follow-up). |
| **IME: full composition** | 🟢 | **Done (2026-08).** `CupriInputConnection` (`BaseInputConnection`) maps SetComposingText/FinishComposingText/CommitText/DeleteSurroundingText onto the engine's new composition seam (see §5's web-IME row — one seam, two hosts); `EditorInfo` follows the focused field's kind (numeric/password/multiline → keyboard layout), the soft keyboard follows engine focus both ways (gate: `mInputShown` flips on field tap and on Escape), and `AdjustResize` keeps the caret visible. Real Gboard composition on a device is the documented manual pass; the engine behaviour is pinned by the headless composition tests. |
| **TalkBack bridge** | 🟢 | **Done (2026-08), gate-proven by a real a11y client.** The fourth bridge, same altitude as the other three: a raw `AccessibilityNodeProvider` (no AndroidX) serving the semantics tree as a virtual hierarchy — ids per structural path (never reused), answers from an immutable post-frame publish, actions queued back to the document thread through the same machinery as a real click, `Offscreen` → not-visible-to-user, explore-by-touch via hover events, `setprop debug.cupriface.talkback off` as the field kill switch. The gate's client is uiautomator (its dump shows ONLY what a provider exposes): named controls with real rectangles, **a tap at the dump's own bounds flips the model and the re-dump reads the new checked state** (the NSA gate's geometry-round-trip lesson), and after a fling the exposed row window has moved. One bug this gate caught before any human could: the change-event throttle had no trailing edge, so Android's client-side node cache served a tree from ~250dp before every fling's settle — TalkBack would have announced the wrong rows. What TalkBack SAYS remains the human pass, same caveat as the other three bridges. |
| **Phone-first sample** (`samples/AndroidViewer` + `MobileApp`) | 🟢 | **Done (2026-08).** `MobileApp` is a PORTABLE app (bottom-nav, virtualised list, IME form, settings, About) that also runs on desktop (`Viewer --app mobile`); AndroidViewer is ~15 lines composing it with the host, and its About page pushes the full desktop **Showcase** onto the app stack (Back returns). The layout is calibrated headlessly at the gate's exact dp geometry — including landscape, where a height-qualified media query keeps phone rows full-width (the engine learned `min-height`/`max-height` media features for exactly this). |
| **No desktop native in the APK** | 🟢 | The engine's convenience props pull desktop Skia natives into every restore; the TFM condition limits what it can (restore-only noise remains — documented in `src/SkiaNativeAssets.props`), and the gate ASSERTS the APK carries none of them. |
| MediaCodec video (`<cupri-video>` on Android) | 🔴 | The engine's surface lane is host-agnostic; an Android leg means MediaCodec decode or codec-RID natives for `CupriFace.Media`. |
| CursorAnchorInfo (IME popup positioning) | 🔴 | The engine already computes the caret rect; wiring it to `CursorAnchorInfo` positions gboard's candidate window precisely. |
| Pinch-zoom gesture | 🟢 | **Done (2026-08) as a SEAM, not a gesture.** `doc.OnPointer` hands an author the raw pointers with web-style capture; the engine computes no pinch, because what a second finger means is the author's decision. See §12. A `Present`-level document zoom is still open. |
| AAB / Play-store shaping | 🔴 | The gate builds signed APKs; store distribution (AAB, per-ABI splits, signing config) is untouched. |
| iOS host | 🔴 | The Android host is the template: same engine, a `CupriFace.iOS` package (CAMetalLayer or GLKit + UIKit touch + UIAccessibility bridge). Nothing engine-side blocks it. |

## 12. Input & interaction (P1)

Everything here was driven by a person holding a phone. Each row is a bug or a gap that no
automated gate could have found, and several turned out to be engine-wide rather than mobile at
all — the device was simply the first place they were reachable.

| Item | Status | Notes |
|------|--------|-------|
| **Two-axis scrolling** | 🟢 | **Done (2026-08).** A box whose content is too wide scrolls sideways, with its own momentum. Mirrored from the vertical path but kept a SEPARATE concept (`IsScrollableX`), because `IsScrollable` means "overflows vertically" and is load-bearing for paint culling, wheel routing and scroll capture. The axes chain independently: a card row inside a scrolling page takes the sideways part of a diagonal drag while the page takes the up-down part. Layout measures the widest child extent; paint, hit-testing and the semantics tree all apply the same offset, because a card scrolled into view that cannot be tapped where it appears is worse than one you cannot reach. |
| **Axis lock** | 🟢 | **Done (2026-08), reported from a device.** Sending both axes on every move meant a sideways drag also crept the page up and down. A gesture now claims the axis it committed to, decided once the finger has travelled far enough to have meant it — deferred rather than decided at slop, because a fling-catch enters scrolling with no movement at all. A genuinely diagonal start still moves both. |
| **Overscroll (rubber band)** | 🟢 | **Done (2026-08), reported from a device**: reaching the end of a list was indistinguishable from the app freezing. Stretches with resistance (the further it goes, the less each pixel gives), bounded at 90px, springs back exponentially on release, and never engages mid-list. Lives beside the fling — an animation the hosts already drive, with state that is a structural path plus a number — and joins BOTH animation gates, the wake gate and the drive gate. |
| **Multi-touch seam** (`doc.OnPointer`) | 🟢 | **Done (2026-08).** Pointer identity end to end, and **capture per target**: a pointer is owned by an element on down and stays until it lifts, which is what keeps a pinch from also scrolling the page underneath — and why there is no `touch-action` arbitration, since capture already solves it. The engine computes no gesture; `e.Pointers` carries every pointer that element holds and the arithmetic is the author's. Built-in `cupri-*` elements stay single-pointer and accessible; anything built on this seam is the author's to make accessible, or not (TOOLBOX §8.1). |
| **Gesture recogniser** (`doc.OnManipulate`) | 🟢 | **Done (2026-08).** Scale, rotation and pan with the focal point already right, layered over the raw seam — every other cross-platform toolkit ships one, and not shipping ours meant every author rewriting the same trigonometry. Two of its lessons came from a phone rather than from review, and TOOLBOX §8.1 now teaches both: **compose, don't assign** (the values are cumulative since the gesture began, so writing them straight into a model resets the content the instant a second grab starts), and **size the gesture surface for fingers, not artwork** (a 90dp tile is ~14mm of glass — two fingertips cannot both land on it, capture is per-element, and the second finger silently falls outside). Emulators and headless tests catch neither, because their fingers are points. |
| **Transform-aware hit-testing** | 🟢 | **Done (2026-08), reported from a device.** The painter had applied CSS transforms since the engine was written and hit-testing never had: it compared the pointer against the LAYOUT box, so a scaled element had a hit area the size it used to be and a rotated one answered near corners it no longer had. An engine-wide bug in any app using `transform:`, found in half a minute by the first feature that changes a transform continuously while you hold it. |
| **Capability signal** | 🟢 | **Done (2026-08).** The host reports what is driving the app — coarse/fine pointer, hover or not — and the engine puts it on the body as `cupri-coarse`/`cupri-fine`/`cupri-nohover`. Adapting is then ordinary CSS. Deliberately NOT `@media (pointer: coarse)`: that means teaching the parser a discrete-keyword shape to reach exactly what the cascade already does with a class. Platform is the wrong axis anyway — a Windows tablet is a desktop OS with a coarse pointer, a docked phone a fine pointer on a mobile one. |
| **The IME contract** | 🟢 | **Done (2026-08).** A soft keyboard could not move our cursor, and it had three independent causes: `setSelection` was never overridden, `updateSelection` was never *called* (so the keyboard's model of the field was empty and it had no offset to move from), and DPAD arrows were dropped. Plus `setComposingRegion` (tap a word to correct it, still one undo step), caps mode, code-point deletes, batch coalescing, extract mode for landscape, the IME's own edit menu routed through the same clipboard seam, and `inputmode`/`enterkeyhint`/placeholder reaching `EditorInfo` — the vocabulary the web host already speaks. |
| **Autofill / password managers** | 🟡 | **Built, NOT yet confirmed on a device (2026-08-18).** A virtual view structure built from the semantics tree — the TalkBack bridge's twin for a different consumer. Only fields the author labelled with `autocomplete` are offered; guessing that something is a password field would be a security bug wearing a convenience costume. Saving needs its own seam: a service fills passively but only offers to REMEMBER when the app declares the form finished (`doc.SubmitForm()` → `AutofillManager.Commit()`). **Why amber:** two device passes showed neither a fill prompt nor a keyboard chip. The cause found on the second was that focus was announced with the VIEW-level `NotifyViewEntered` while the fields live in a virtual structure, so Android never learned which field had focus and no fill session ever started. That is fixed (stable path-keyed virtual ids, per-field enter/exit with screen bounds, `NotifyValueChanged` on typing) but is **unverified**: no emulator carries a password manager, so CI cannot answer this one. See *Parked: verifying autofill* below. |
| Web host: touch + capability | 🟢 | **Done (2026-08-18).** Both web hosts (raw-WASM and NativeAOT-LLVM) now route fingers to the same `TouchInput` the Android host uses — activation on RELEASE, 8px slop, momentum, long-press, axis lock, rubber band — while a mouse keeps the desktop path, split on `pointerType`. Raw-pointer elements capture their finger first, so `OnPointer`/`OnManipulate` gestures work in a browser exactly as they do on a phone. `touch-action:none` is load-bearing: without it the browser keeps every gesture for its own scrolling and `pointermove` stops arriving the moment a finger travels. The capability signal follows the pointer in USE rather than the device, because a laptop with a touchscreen is honestly both. Long-press needs no JS timer — the recogniser names its next deadline and the frame loop, already running, asks. **Proof:** verified in Chromium under Pixel-7 touch emulation — a held press changed nothing and activated on release, a drag tracked 17/17 frames and coasted after the lift, the profile went coarse→fine→coarse as the pointer changed, and a 500ms hold raised the context menu. NOT yet a blocking gate. The follow-up is a BROWSER gate (Chromium under touch emulation, driving the page), not a headless one: the repo's node harness talks to the C# exports directly and would skip the entire JS layer — pointerType routing, `touch-action`, pointer capture, the profile switch — which is exactly where this feature lives and where it was broken. A green headless run would have proved almost nothing while looking like proof. |
| Document-level pinch zoom | 🔴 | An accessibility-shaped feature rather than a gesture one: two fingers scaling the whole page via `Present`, distinct from an element's own transform. |
| `commitContent` (keyboard GIF/stickers) | 🔴 | Needs an image sink and an `EditorInfo.contentMimeTypes` opt-in. A feature rather than contract-completion. |

**A note on proof.** The IME contract and autofill are the features here that CI structurally
cannot drive: it cannot type on a real keyboard or install a fill service. Their engine halves are
covered by headless tests; the platform halves rest on a documented manual pass. That is stated
rather than papered over — and it is why the phone sample carries a surface for each of them (a
gesture tile, a sideways row, autofilled fields), so the manual pass has something to press.

**Multi-touch used to be on that list and no longer is.** `adb shell input` is single-pointer, which
is why the seam shipped without a gate — and a device then found a two-finger bug CI could not have
caught. The gate now speaks the multi-touch protocol itself and elects its transport empirically
(the emulator console; `sendevent` is permission-denied to the shell user, and the emulator exposes
eleven virtio touch devices of which one drives the display). It proves each link separately: a
trusted single tap, a canary that finds the live device, both fingers landing together, a second
finger arriving late, and the same pinch repeated. Five CI rounds to build, and the lesson worth
keeping is that every one of those rounds failed for a reason inside the harness rather than inside
the product.

### Parked: verifying autofill

Implemented, plausible, unproven — and it needs a human with a password manager, so it is written
down rather than left in someone's memory. On a device with a manager installed (Vaultwarden,
Bitwarden, 1Password…), running the phone sample's **Form** page:

1. Tap **Email**. Expect the manager's inline chip in the keyboard strip, or a fill dropdown. This
   is the load-bearing signal: it means a fill SESSION exists. Everything else depends on it.
2. Fill both fields (by hand is fine) and press **Sign in**. Expect an offer to SAVE.
3. If neither appears, `adb logcat -s cupri:I` while tapping the field shows the host's focus and
   pointer markers — whether the announcement fires at all is the first fork in the diagnosis.

Known unknowns if it still fails: whether the manager uses the Autofill framework at all (some use
an accessibility service instead, which is a different integration), and whether `SubmitForm()`
reaches `Commit()` before the field values have been reported to the session.

---

## Non-goals (won't do — by design)

Per [DESIGN.md §1](DESIGN.md); listed here so "deferred" is never confused with "planned but missing":

- **A JS engine in the runtime**, or JavaScript in app UI pages. (The web target's non-authored
  bootstrap/interop glue is expected and separate.)
- **100% CSS spec compliance** — we chase the modern core and consciously skip legacy cruft:
  `float`-based layout, multi-column, print/`@page`, obscure pseudo-classes.
- **A full DOM `document.*` scripting API** — there's no script to call it.

---

## Operational (not a feature)

- **nuget.org publishing** — packages build and attach to GitHub Releases; pushing them to
  nuget.org needs a `NUGET_API_KEY` repository secret (then ~4 lines in the release job).
